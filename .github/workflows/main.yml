on:
  push:
    branches:
      - master







jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'Testing githup actions'
        run: pwd

      - name: 'Testing printing'
        run: echo 'mahmoud'

